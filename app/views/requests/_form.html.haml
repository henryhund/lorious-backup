%h3 
  Thanks for signing up,
  =@name
  !
%h5
  We aren't quite ready yet. In the meantime, let us know how we can help.

= simple_form_for(@request) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, as: :hidden, input_html: { value: @name }
    = f.input :email, as: :hidden, input_html: { value: @email }
    = f.input :expertise, as: :hidden, label: "Your expertise", input_html: { value: @expertise }
    = f.input :help_needed
    = f.input :time_needed, collection: ["< 15 minutes", "15-30 minutes", "30-60 minutes", "1 hour+"]
    = f.input :max_rate

  .form-actions
    = f.button :submit
